/*
 * phase1_minimal_harness/app.js — Phase 1 deliverable.
 *
 * WHAT TO IMPLEMENT
 * ------------------
 * - connectToRoom(token): LiveKit room connection + local mic publish.
 * - onTrackSubscribed(track): attach agent's TTS audio track to <audio>.
 * - renderLogEvent(event): append structured log events to the on-page panel
 *   (see index.html). Keep this dumb — it just displays what the server logs.
 *
 * NOT YET (do not build here — these belong to later phases):
 * - VAD / local ducking            -> client/src, Phase 3
 * - Interruption UI feedback       -> client/src, Phase 4+
 */

// TODO(phase-1): implement connectToRoom, onTrackSubscribed, renderLogEvent
